# To-do for 2026-06-24

Outstanding items after the deliverability + meeting-followup session. Most of the
deliverability ones are client-blocked and watched automatically by **LES-deliv-verify**
(runs every 4h, emails info@ the moment each resolves, then disables itself).

## 1. lk-advertising DMARC fix (CLIENT-BLOCKED, watched) — Lukas
- lk's DMARC is broken: TWO conflicting `_dmarc` TXT records on lk-advertising.site
  (one `p=none`, one `p=quarantine`) -> receivers report `permerror` -> lk's mail is at
  real spam risk at Gmail/Yahoo. This is the ONE client not at max deliverability.
- FIX (Lukas only — it is his Hostinger DNS, we have no token): delete the short
  `v=DMARC1; p=none` record, keep the longer `p=quarantine` one.
- Emailed lukas@weltmonarch.de 2026-06-24. No self-serve workaround (lk has zero
  DMARC-covered subdomains to fall back to). The watch alerts info@ the instant it lands.

## 2. ENER-G org-level DMARC (CLIENT-BLOCKED, watched, AUTO-RESTORES) — Philipp / NS1
- energ had 10/12 sending subdomains with no DMARC. STOPGAP already applied today: energ
  now sends only from its 2 DMARC-covered subs (hello, team), so 100% of its mail is
  authenticated. Cost: volume capped to ~100/day until the real fix lands.
- REAL FIX (needs energ's NS1 DNS manager): add one TXT record
  `_dmarc.ener-g-beratung.de` = `v=DMARC1; p=none; rua=mailto:dmarc@ener-g-beratung.de`.
  Emailed philipp.loisha@gmail.com 2026-06-24 (DNS is on NS1/nsone.net, we have no creds;
  he may need to forward to his DNS person).
- When it resolves, LES-deliv-verify auto-restores all 12 energ subdomains (restore_energ).

## 3. Philipp's Gmail placement reply (AWAITING) — Philipp
- Sent 6 seed emails (one per live domain) + a heads-up to philipp.loisha@gmail.com asking
  him to reply with inbox-vs-spam per sender. This is the decisive Gmail-folder confirmation
  (the one signal no API can give us). His reply lands in info@; the watch flags it.

## 4. Commit this session's changes (ME) — DONE 2026-06-24
- sequences/meeting-followup.py + sequences/reply-review.py were ALREADY committed earlier
  (b67b372, 5693e0b) — the popup, skip-vs-decide-later, and the PowerShell-injection /
  duplicate-send-race / task-kill / send-identity hardening.
- profiles/energ.json (energ sub restriction) + scripts/verify-deliverability-fixes.py
  (the LES-deliv-verify watch) + this TODO committed 2026-06-24, staged individually.
- NOTE: the `LES-meeting-followup` ExecutionTimeLimit PT0S change is live config, NOT
  version-controlled (would revert if that scheduled task is ever recreated from a script).

## 5. lk-advertising on-brand footer address — Lukas
- LK email footer shows lukas@weltmonarch.de (works, but off-brand) because
  info@lk-advertising.com does not exist. Ask Lukas to create that mailbox on his Hostinger,
  then switch the LK brand.legal.contact_email back to it.

## 6. Revoke f2-malergipser's Resend key — DONE / not needed (verified 2026-06-24)
- Checked the Resend account directly: NO f2/malergipser sending domain exists, and NO
  f2-named API key (only 3 generic keys: "New API key for email", "Test", "Onboarding").
  Nothing to revoke; it was never created or already cleaned up. Closed.

## Done today 2026-06-24 (reference)
- Full deliverability audit: 5/6 clients + aureonglobal verified at MAXIMUM (SPF+DKIM+DMARC
  pass at a real receiver, 0% complaints, low bounce, 6/6 seeds to inbox). Verdict: mail is
  NOT secretly going to spam; lk is the only genuinely broken sender.
- Fixed the meeting-followup missing draft-approval popup; added Skip vs Decide-later; hardened
  a real PowerShell injection reachable from a prospect-controlled calendar invite, a
  duplicate-send race, and a scheduled-task mid-dialog kill. Same injection fixed in reply-review.py.
- Sent mark-eting's latest portal contract to mark@mark-eting.co.
- Restricted energ to its DMARC-covered subdomains; built + scheduled the LES-deliv-verify watch.
