# local-email-stack Python deps — pinned conservatively to stay in sync
# with what works on the Windows dev host. Bump as a unit when upgrading.
httpx==0.27.2
playwright==1.49.0
pyyaml==6.0.2
beautifulsoup4==4.12.3
dnspython==2.7.0
ddgs==9.0.0
imapclient==3.0.1
