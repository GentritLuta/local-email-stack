# To-do for 2026-06-12

Deferred from 2026-06-11. All queued as Claude task chips — one click each:
LK (task_dc15be21), ENER-G (task_c4b0499f), AlgoAlpha retainer rework
(task_f6556ae1), SocialForge social (task_de045013), client contracts (task_2e9c1e06).

## 5. Re-edit + send contracts to every client (NOT Diraya, never Laso)
- Clients: Dorian/Mercury Scales, LK, ENER-G, AlgoAlpha (+ any other active). EXCLUDE
  Diraya (already signed: Diraya-Pilot-Agreement.pdf). EXCLUDE Laso (active legal case).
- Templates: C:\Users\bernh\Aureon-Contracts\ (pilot agreements) + Downloads (MSA, DPA/AVV,
  GTC, Commission Agreement). Existing drafts: LK Commission Agreement, AlgoAlpha DL
  Agreement + DPA.
- Re-edit party details + scope + pricing/retainer/commission + DPA per client, show
  Gentrit, then SEND ONLY AFTER his explicit per-contract approval (irreversible, client-
  facing). Confirm the correct Aureon ARBK/VAT numbers first (agreement-vs-footer mismatch
  flagged in the Laso memo). AlgoAlpha: support@algoalpha.io bounces — get a working address.

## 3. Rework AlgoAlpha offer -> per-video retainer (formula)
- Current offer = "30% lifetime commission" (affiliate) in sequences/algoalpha-default/variants.json.
- New: concrete RETAINER PER VIDEO from a formula tied to creator reach (avg views / subs).
- FIRST get the formula from Gentrit (base rate, tiers, cadence, replace-or-stack with
  commission, floor/cap). Then rewrite the 7-email Hormozi sequence; auto-compute a
  {retainer} merge per creator if the scraped view/sub stats allow (email_template_algoalpha.py).
- Keep algoalpha-pitch as the A/B alt (commission vs retainer). Proof points: 14.1M chart
  adds, 75k paying traders, Editor Choice x2.

## 4. Advance SocialForge social media
- Grow SocialForge's social presence. SCOPE WITH GENTRIT FIRST (goals, platforms, current
  state). Likely codebase: C:\Users\bernh\x-api (X/Twitter API integration).
- No public posting without approval of content + accounts.

## 1. Build LK Advertising's 12 sending subdomains
- LK (`lk-advertising`) rides aureonglobal.de subs today; scrape + report already wired.
- BLOCKER: lk-advertising.site is Lukas's Hostinger account — need his Hostinger API
  token (hPanel -> Account -> API -> Generate), like Dorian gave.
- Then: add HOSTINGER_API_TOKEN_LK_ADVERTISING to sequences/hostinger.env, provision 12
  subs on the PRO Resend account (re_iPPp8fht), push DNS via his token, add 12 personas
  to profiles/lk-advertising.json, verify. Pattern = how Dorian's 12 were built.

## 2. Fix ENER-G DNS so its 8 domains verify
- ENER-G (`energ`) 8 subs (hello+team on ener-g-beratung .de/.org/.com/.store) created on
  PRO but stuck 'pending'. Scrape + report (LES-report-energ -> info@) already wired.
- Likely cause: the ener-g-beratung.* nameservers don't point to the Hostinger zone that
  HOSTINGER_API_TOKEN_ENERG controls, so pushed records never resolve.
- Diagnose NS for each domain, confirm records published, then tell Philipp
  (philipp.loisha@gmail.com) exactly what to change. Verify + optionally expand to 12.

## Done 2026-06-11 (for reference)
- AlgoAlpha fixed: stall kicked (was 34h idle), support@algoalpha.io bounce -> report
  reverted to info@, 3 junk leads purged, YouTube miner re-enabled, daily report live
  (LES-report-algoalpha 08:15 -> info@). All 12 subs confirmed warming evenly.
- Dorian completed to full 12-subdomain kit on PRO (+10 subs/personas).
- Confirmed Aureon Resend account = Transactional Pro, 100,000 emails/mo ($35).
- Aureon real_estate_us queries broadened 14 -> 34 to lift the 3x buffer yield.
